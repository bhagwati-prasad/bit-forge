const throttle = (() => {
})();

const debounce = (() => {
})();

const request = (() => {
    const setHeader = (header, value) => {
    }
    const get = (url) => {
    }

    const put = (url, data) => {
    }

    const post = (url, data) => {
    }

    const delete = (url) => {
    }

    return {
        setHeader,
        get,
        put,
        post,
        delete
    }
})();
